const apiBaseUrl = "https://api.tcgdex.net/v2/en";

const endpoints = {
  sets: `${apiBaseUrl}/sets`,
  cards: `${apiBaseUrl}/cards`,
  types: `${apiBaseUrl}/types`,
  categories: `${apiBaseUrl}/categories`,
};

export default endpoints;
